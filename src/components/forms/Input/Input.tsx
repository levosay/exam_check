import { FunctionComponent } from 'react'
import { IInputProps } from './Input.d'

export const Input: FunctionComponent<
  IInputProps
> = (): JSX.Element => {
  return <div className={''}>Input Component</div>
}
