import { FunctionComponent } from 'react'
import { IHeaderProps } from './Header.d'

export const Header: FunctionComponent<
  IHeaderProps
> = (): JSX.Element => {
  return <div className={''}>Header Component</div>
}
